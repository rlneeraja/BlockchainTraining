contract('AutoFareCalculator2', function(accounts) {
  it("should assert true", function(done) {
    var auto_fare_calculator2 = AutoFareCalculator2.deployed();
    assert.isTrue(true);
    done();
  });
});
